# V4.6.5 — 大点云高性能显示 / Raster LOD

- XY 自动采用全量显示源 Raster；支持高度、点密度、缺测中性背景。
- 缩放/平移后重新生成局部栅格；200 ms 防抖，后台计算与旧结果拦截。
- 3D/XZ/YZ 使用空间代表点及强制保留的原始Z/平面残差极值。
- 新增源有效点、分析输入、最终计算、显示规模链路；未知不冒充精确值。
- Smart ROI 点击真实源点，不依赖抽样点或栅格平均高度。
- 报告图复用 Raster/LOD；Recipe schema 8 增加可选显示字段，兼容旧配置。
- 窄窗口指标自动换行、四视图纵向滚动；修复姿态按钮符号缺字。

Rx/Ry/PV/TTV/RMS/Mean Z、拟合、滤波、ROI/Smart ROI、胶厚、平行度和装调定义不变。
密度与缺测均为显示诊断，不自动判定孔；导入抽样后无法恢复原文件覆盖率。
范围内无点的像素不插值填洞。小于当前栅格尺寸的缺测仍可能不可见，需要放大。
源文件跳读路径的总有效点数可以为未知，不会为统计而偷偷再扫描整文件。

当前交付为本地源码；未上传 GitHub，未发布安装包。Windows Python 启动器沿用项目 .venv。
