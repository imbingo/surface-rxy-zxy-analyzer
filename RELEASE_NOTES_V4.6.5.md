# V4.6.5 — 大点云显示与操作优化

- XY 总览采用全显示源 Raster；放大后自动显示原始散点，缩放保持同一高度色标。
- 无有效数据区域透明，不插值、不补洞。孔区与缺测均表示无数据，不自动判定物理孔洞。
- 3D/XZ/YZ 使用空间 LOD，保留关键极值点；显示抽样不改变分析点集和量测定义。
- 数据规模链路统一在底部显示，区分源数据、分析输入、最终计算和显示数量；未知源点数不冒充精确总数。
- 移除 XY 图上方下拉栏，分辨率通过右键菜单设置；恢复矢量姿态图标。
- 修复 ROI 框选边界撑大坐标范围、压扁有效曲线的问题。
- 保留 V4.6.4 装调、Recipe 与导出功能；Recipe schema 保持 8，旧显示模式兼容回退高度图。

## 安装

Windows x64 完整安装包，内置 Python 与依赖，无需另装 Python。
默认管理员安装，也支持安装程序的当前用户选项。支持从 V4.6.4 升级，保持既有 AppId。
未进行代码签名，Windows SmartScreen 可能提示未知发布者；请核对附件 SHA-256。

## Demo

`demo_holes_30x30mm_XYZ_mm.dat` 为合成测试数据，不是真实仪器标定数据。
338,739 点，XYZ 全部为 mm，30×30 mm 范围，中央直径 6 mm 孔、右上直径 3 mm 孔及左侧缺测条带。
导入选择 XYZ，三轴单位 mm，建议全量导入并关闭 ROI/滤波查看。

源代码回归：174 项测试通过。显示优化不改变 PV、Rx、Ry 等指标计算口径。
