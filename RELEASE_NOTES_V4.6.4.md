# V4.6.4 — 平行度装调补偿 / N 点垫片计算

- 平行度分析新增“4 装调计算”，支持三点、四角模板及任意 N 点支撑布局。
- 直接复用当前/目标平面系数，提供支撑点几何中心、坐标原点和自定义装调原点。
- 当前误差 E=Current−Target；理论调整 H=Target−Current，正数加片、负数抽片。
- 支持基础垫片、厚度上下限、固定步进量化及预计残余 Rx/Ry/DZ0。
- 支撑共面 PV/RMS、最大偏离和最差点；多点过约束及不共面警告。
- 可编辑支撑表、三维补偿预览、装调 CSV 和复制结果。
- Recipe schema 8 保存工装与装调规则，不保存 DUT 动态结果；兼容旧 Recipe。
- 修改输入、更新主控处理、换面、交换、导入 Recipe 后清除旧装调结果。
- 主控 3D 选中点采用前景标记，随视角旋转仍可见，真实 XYZ 不变。

## 数学与使用边界

现有 Rx/Ry、PV/TTV/RMS/Mean Z、平面拟合、ROI、Smart ROI 和胶厚数学定义不变。
装调 DZ0 为指定原点处两个拟合平面的高度差，不是 Mean Z 差。
三点和四点共用 N 点算法。量化对最终厚度取最近值，半步远离零，不静默截断超限值。
N≥4 的预测为最小二乘几何预测，不包含刚度、局部平面度、预紧力和实际接触力学。
调整点必须为真实支撑/垫片接触位置，且与拟合面使用同一坐标系。
面槽保留设置时快照；主控处理变化后，应重新设置对应面并计算。

## 兼容与安装

- Windows x64；安装包内置 Python 和依赖，无需安装 Microsoft Excel。
- 保持原有安装 AppId，支持从 V4.6.3 覆盖升级。
- 默认管理员安装，可选择当前用户安装；用户配置和外部测量/Recipe 文件保留。
- schema 8 Recipe 无法由旧 schema 7 应用加载；升级前保留原 Recipe 文件。
- 当前未数字签名，Windows SmartScreen 可能提示未知发布者，请核对 SHA-256。

## 验证与追溯

包含数学符号、连续调整归零、原点 DZ、量化预测、N 点几何、共面性、Recipe、
状态失效、导出、3D 选中点和已有分析路径的回归测试。
安装包提供 `--check` 与 `--check-adjustment --output-json <path>` 自检。
精确源码提交与安装包 SHA-256 记录于构建信息、update_manifest.json 和校验文件。
