# Surface Rxy ZXY Analyzer

面型及 Rxy 分析工具，当前主程序已更新到 V3.8.4。

## 文件

- `面型及Rxy分析ZXY版.py`: PyQt6 GUI 主程序，内容为 V3.8.4 最新版。
- `start_surface_analyzer_v3_8_4.bat`: 推荐启动脚本，固定使用仓库内 `.venv`，避免换解释器后重复装依赖。
- `requirements.txt`: 运行所需 Python 依赖。

## 运行

```powershell
.\start_surface_analyzer_v3_8_4.bat
```

首次运行会创建 `.venv` 并安装依赖；后续会复用同一个环境。需要只检查环境、不打开 GUI 时：

```powershell
.\start_surface_analyzer_v3_8_4.bat --check
```

## V3.8.4 重点

- **主控分析新增 ROI 区域**：支持矩形 ROI 和圆形 ROI，只分析 ROI 内数据；多个 ROI 按并集合并。
- **两种生成方式**：可在 XY 俯视图鼠标框选，也可输入中心坐标、长宽或半径精确创建。
- **处理顺序固定**：手动删除点 → ROI 保留 → 自动滤波 → 平面拟合，关闭 ROI 时旧文件分析口径不变。
- **Recipe 与报告记录 ROI**：Recipe 会保存 ROI 开关和形状；CSV 与报告图会写入 ROI 摘要，XY 图会叠加 ROI 边界。
- **批量处理支持 ROI**：批量处理沿用当前 ROI/Recipe ROI，但仍不包含手动框选删点。

## V3.8.3 重点

- **大文件默认使用空间网格均匀采样**：超过阈值的 TXT / DAT / ASC / XYZ 会按 X/Y 网格采样，不再默认按文件位置抽行。
- **每格保留极值**：每个网格最多保留首个代表点、Z 最小点、Z 最大点，降低 TTV / PV 因采样丢失局部极值的风险。
- **网格数可自动或自定义**：大文件策略里新增“空间网格数”，0 表示自动；实际使用的网格数、占用网格数会写入导入说明。
- **保留旧采样方式**：大文件策略里仍可切换到“文件位置均匀采样”，用于兼容旧版导入口径。
- **平行度报告排版优化**：导出的报告图左侧改为报告信息、平行度结果、单面拟合指标、口径说明四段式布局，文字更清楚。

## V3.8.2 重点

- **平行度分析保持 3D 分面预览**：基准面和测量面分成两张 3D 图，点云按 Z 高度着色，并叠加半透明拟合面。
- **平行度左侧 UI 修复**：基准面、测量面、计算导出改为主控页同款分步布局，避免普通 GroupBox 标题乱放。
- **平行度导出升级为报告图**：原“导出预览图”改为“导出报告图”，包含平行度结果卡片、基准/测量指标表和两张清晰 3D 拟合预览。
- **多层胶厚扣减新增匹配诊断图**：以堆叠总成 XY 点为基准，显示成功匹配、未匹配单片1、未匹配单片2、两层都未匹配的点。
- **Gap 结果仍写入主控分析**：计算成功后右侧先显示匹配诊断；切回“单层 / 主控分析”即可查看扣减后的 Gap 面型四视图。

## V3.8.1 重点

- **大文件策略新增三档模式**：快速 / 标准 / 精确。
- **快速模式**：128MB 起触发抽样，最多导入 15 万行，最多显示 4 万点，适合先快速确认格式和面型趋势。
- **标准模式**：512MB 起触发抽样，最多导入 50 万行，最多显示 8 万点，延续 V3.8.0 默认口径。
- **精确模式**：512MB 起触发抽样，最多导入 150 万行，最多显示 15 万点，适合最终复核但导入和绘图会更慢。
- **保留手动参数**：手动改阈值、导入上限或显示上限后会显示为“自定义”；Recipe 会保存当前模式和实际参数。

## 姿态变换与 Rx/Ry 物理正确性（已核验）

本机台 **Z = 厚度（标量）**，在任何重新装夹 / 翻面 / 镜像下都**不变号**；因此所有姿态变换
只重排 (x, y)、保持 z 不变，物理上是正确的。以物料坐标系为基准输出：物料在整机里的位姿是
固定真值，姿态变换是把实测数据搬回该固定坐标系，变换后 Rx/Ry 是同一物理楔形在新工件轴向下
的正确再表达（`Rx = arctan(+dZ/dY)·1e6`，`Ry = arctan(-dZ/dX)·1e6`）。

七个变换全部数学自洽 + 物理正确（`|tilt| = sqrt(Rx²+Ry²)` 在所有变换下恒定不变）：

- **旋转** 顺时针90° / 逆时针90° / 旋转180°（det=+1）：工件在平面内转着重新装夹。
- **反射** X-Y对调 / 前后翻转 / 左右翻转（det=−1）：工件绕**面内轴翻面**。
  对面高（干涉）测量翻面会使 Z→−Z、裸 XY 镜像会给错符号；但本机台 **Z=厚度不变号**，翻面就等于
  「XY 镜像 + 厚度不变」，正是这三个反射所做的事 —— 故对本机台**合法且正确**。
- **平移** 平移归零：只改截距，倾斜不变。

闭合性：顺90°×4 = 恒等、顺90°×2 = 旋180°、前后翻转 + 左右翻转 = 旋180°。

用法注意：① 选对变换要对准实际装夹 / 翻面动作（`|tilt|` 幅值在任何变换下恒定，抓不出"按错按钮"
导致的 90° 偏差）；② Rx/Ry 的**绝对**正负号需用已知楔形方向的标准件实测标定一次（工具给的是自洽
的相对关系）。

## V3.8.0 重点

- **新增「平行度分析」页**：从主页面已处理后的当前数据写入基准面和测量面，复用单位、旋转/翻转、滤波和手动删点结果。
- **不做对应点相减**：两个文件可以是不重叠区域；分别拟合 `Z = aX + bY + c` 后比较 `ΔRx = Rx测量 - Rx基准`、`ΔRy = Ry测量 - Ry基准`。
- **结果输出**：基准面/测量面分别输出 Rx、Ry、RMS、PV、TTV、平均 Z，并输出 ΔRx、ΔRy、合成夹角。
- **双 3D 静态预览**：基准面和测量面分成两个图显示，避免两个面高度接近时互相遮挡。
- **主页面保持 V3.7 布局**：新增页面通过第三个 Tab 进入，单层主控和多层胶厚扣减页面不做重排。

## V3.7.0 重点（界面更新 · 对齐设计稿方案 A，纯 UI/样式，不改动任何算法）

- **四视图卡片化**：四幅图改为 2×2 四张独立卡片（白底圆角 + 柔和投影 + 留缝），模块感更强；
  子图标题改用 Qt 渲染的「● 标题」，蓝点与文字对齐。
- **模块化投影**：顶部读数卡、姿态磁贴、绘图卡均挂柔和投影，呈悬浮模块质感（车机 / Apple 风）。
- **顶部常驻读数条**：平均厚度 Z、面型 PV·法向、TTV·Z 极差、Rx、Ry 与平面方程上提为绘图区上方
  读数卡，淡底色 + 加大加粗数值强化读数，PV 卡蓝色高亮；去倾斜显示开关并入标题行，节省空间。
- **左栏分步引导**：工作流改为「1 载入 → 2 列映射 → 3 姿态 → 4 滤波」编号分步；姿态变换改为
  带线性图标的方块磁贴（↻ ↺ ⟳ ⇄ ↕ ↔ ⊕ ↶）。
- **统一中性风**：去掉全部 emoji 与多彩按钮，统一中性灰阶 + 单一强调色 `#2F6DB0`；自绘 chevron
  替换 Windows 原生下拉 / 微调箭头；删除 / 危险操作用低饱和红描边。
- **无边框自绘标题栏**：顶栏拖动移动 / 双击最大化、边缘拉伸缩放、最小化 / 最大化 / 关闭按钮。
- 说明：所有指标计算口径、数据流、滤波算法、Gap 运算、Recipe、批量处理、CSV 导出与报告图逻辑
  均保持不变；本版仅重排界面与样式。

## V3.6.0 重点

- **新增第 4 档滤波【迭代 σ 裁剪】**：反复用最佳拟合平面残差的标准差 σ 裁掉
  `|残差-均值| > Nσ` 的点并重拟合，直到残差 std 收敛或无新增剔除（单调裁剪，只剔不回收）。
  σ 倍数与迭代上限可配置（默认 3σ / 5 次）。适合“基本是平面 + 少量毛刺”的工件。
  注意：本档对全局残差判异，面有真实弧度时会削减 PV，弧形面建议优先用局部中位数滤波。
- **新增【批量处理】**：导入时可多选文件，沿用当前界面（或已导入 Recipe）的列映射 / 单位 /
  旋转 / 滤波设置逐个处理；每个文件输出一张含主页面全部信息（指标 + 四视图）的报告图
  `result_<原文件名>.png`，并汇总生成 `result_batch_summary.csv`。
  批量仅用自动滤波（无手动框选），要求所有文件为同一设备、同样列格式。
  建议先载入其中一个文件调好参数（或导入 Recipe），再点【批量处理】。

## 历史版本（V3.5.x）

- 未载入数据时切换去倾斜显示不再触发重绘异常。
- 局部中位数滤波改为分块近邻查询，降低大点云内存峰值。
- 支持超大 TXT / ASC / XYZ 文件按文件位置预抽样导入。
- 支持 Zeiss 文本常见缺测值如 `***`、`--`、`NA` 按空值处理。
- 导入状态会显示文件大小、导入方式、导入点数和显示点数。
- 绘图显示抽样上限可配置，指标按导入后的分析数据计算。
- Recipe 导出/导入可保存单位、列映射、旋转组合、滤波参数、显示和大文件策略。
