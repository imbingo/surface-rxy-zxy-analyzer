# V4.6.3 Release Notes

V4.6.3 收紧多层胶厚 XY 配准的操作语义：人工拖动结果可直接用于胶厚计算，自动匹配只作为当前位置附近的可选精修；胶厚报告与主控页共用最终 Inner Gap 点云的平面指标定义，并为配准图补充纯显示缩放。

## 主要变更

- 双层和三层胶厚扣减都直接读取单片层当前 `offset_x / offset_y` 建立最终 `cKDTree`，无需先点击自动匹配。
- 自动匹配只以当前偏移为唯一 ICP 初值，移除重心候选；局部自动改变量限制为 `max(3×tolerance, 0.01 mm)`，越界、无效、失败或取消均保留原人工偏移。
- 自动结果只用当前人工位置附近、距离不超过局部保护半径的单片到堆叠候选点求解，并按固定候选集残差与容差内匹配点数验收；当堆叠基准更大、带孔或只有局部相交时，非重叠区域不再把 ICP 拉偏。全点双向 RMS 继续保留为诊断量。
- 每个单片层记录 `none / manual / auto` 配准方式，并在界面状态、CSV 和报告中追溯。
- 胶厚报告显示最终 Inner Gap 拟合平面的 Rx / Ry、PV、RMS 与 TTV，直接复用主控页 `compute_plane_metrics`；CSV 元数据增加 `Gap_Rx_urad / Gap_Ry_urad`。
- 胶厚 XY 配准图支持光标中心滚轮缩放，拖动、切层、容差刷新、自动精修和结果刷新不会重置视野；左键双击恢复初始全图。

## 保持不变的口径

- `Inner Gap = 堆叠总成 - 单片1 [- 单片2]`。
- XY 匹配仍为最近邻欧氏距离 `sqrt(ΔX² + ΔY²) <= tolerance`。
- 自动配准仍只做 X/Y 平移，不旋转、不修改 Z，保留最多 30 轮、局部改变量保护与双向最近邻 RMS 诊断；基准索引使用全量，移动点最多抽样60,000点求解，结果按全量移动点验收。
- 主控 Rx/Ry、PV、TTV、RMS、Mean Z、平行度、Smart ROI 与粗到细抓面算法均未修改。

## 验证范围

- 新增人工直算、局部自动保护、配准方式、Gap Rx/Ry 数值一致、滚轮缩放、缩放后拖动和双击复位回归。
- 专项与完整自动回归共 132 项测试、17 个子测试通过；新增与截图一致的 17,260 点外圈基准 + 2,821 点圆片局部重叠回归，以及选层和拖动前后屏幕 XY 等比例与像素位置稳定性回归。
- 修复选择/拖动单片重建坐标轴后丢失 XY 等比例设置的问题，保留缩放范围时也恢复物理等比例显示。
- 本版本当前阶段仅完成源码、测试、UI 验收和本地提交；未构建安装包、未推送、未打标签、未发布 GitHub Release。

## 审查问题修复（版本号不变）

- 胶厚CSV/报告的指标固定来自其原始有效匹配点快照；明确不包含主控后续旋转、ROI或滤波。
- 旧胶厚因拖动、参数变化或清空而失效后，主控禁止导出该旧结果并提示重算。
- 公共平面拟合检查共线/退化分布，主控清除旧指标，平行度提示无效，胶厚应用前校验，批量输出具体失败原因。
- 大点云保留全量基准索引，移动点按空间顺序确定性采样，避免文件行顺序改变对应关系。
- 新增6项审查修复回归，完整测试共138项通过。前述132项为本轮修复前基线。
