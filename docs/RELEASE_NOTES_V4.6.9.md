# V4.6.9 界面与报告文字排版修复

- 主控四视图保留最小可读高度，小窗口通过滚动显示，避免压缩坐标文字。
- 3D 图预留轴标签边距，减少 X/Y 刻度密度，调整 Z 轴标签间距。
- 单次与批量面型报告按实际字体宽度换行，按内容高度排列来源信息、结果和脚注；长内容自动增加报告高度。
- 报告标题保留版本，完整文件名放入可换行的来源信息中。
- 测量计算、滤波和数据格式未变更。

运行根目录 `start_surface_analyzer_v4_6_9.bat`。这是源码版，需要 Python；首次运行会安装项目依赖。
