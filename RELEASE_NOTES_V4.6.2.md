# V4.6.2 Release Notes

V4.6.2 重新设计多层胶厚扣减工作流，保留原有页面入口与最终扣减公式，并把计算前的 XY 配准变成可视、可调整、可追溯的步骤。单面分析、平行度、ROI/Smart ROI 与既有面型指标定义不变。

## 主要变更

- 多层胶厚页使用三层 XY 叠加视图：堆叠总成是不可移动的灰色基准，单片 1 为蓝色，单片 2 为紫色。
- 选择单片 1 或单片 2 后，可在图内按住左键拖动做粗对齐；红色空心圈实时表示当前层与堆叠层的 XY 最近邻欧氏距离不大于“坐标对齐误差窗口”。
- 新增自动匹配：只优化单片层 X/Y 平移，使用稳健 ICP 并以双向全点最近邻残差 RMS 为目标；不会旋转点云，也不会修改 Z。
- “容差匹配点云并计算胶厚”使用保存后的配准位移。任何层数据、配准位移或容差改变都会使旧胶厚会话失效，防止误导性导出。
- 胶厚页新增导出 CSV、导出报告图和清空当前。CSV 包含各层对齐坐标、Z、匹配索引和匹配距离；报告图包含层配准和 Inner Gap XY 分布。
- 对大点云的手动拖动加入绘制节流与固定源点 KD-Tree 缓存，最终胶厚计算仍使用全量容差匹配。

## 口径与兼容性

- 公式保持 `Inner Gap = 堆叠总成 - 单片1 [- 单片2]`。
- XY 对齐判定为 `sqrt(ΔX² + ΔY²) <= tolerance`，单位为 mm。
- 自动配准与手动拖动只产生单片层的 `ΔX / ΔY`；坐标方向仍需先在主控页通过旋转、翻转等姿态变换统一。
- Recipe 仍为 schema 7；公共分析 API 与 V4.6.1 导入行为不变。

## 验证与安装

- 完整自动回归：121 项通过。
- 已验证 1366×768 与 1600×900 的填充状态界面，中文字体、主要操作、图例和点云显示正常。
- Windows 10/11 x64；安装程序默认可覆盖升级原有版本并保留用户目录中的设置。
- 安装包未做商业代码签名，Windows SmartScreen 可能显示“未知发布者”；请用随附 SHA-256 文件校验完整性。
